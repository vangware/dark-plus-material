/**
 * Distance between two values.
 * @param value1 Some numeric value.
 * @param value2 Other numeric value.
 */
export const distance = (value1: number, value2: number) =>
	Math.abs(value1 - value2);

export default distance;
