/**
 * Example JSDoc
 * @class Foobar
 */
export class Foobar {
	/**
	 * Creates an instance of Foobar.
	 *
	 * @param {string} foo
	 * @param {string} bar
	 */
	constructor(foo: string, bar: string) {
		console.log({ foo, bar });
	}
}
