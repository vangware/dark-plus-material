/**
 * Example JSDoc.
 *
 * @param {string} foo Example description for foo.
 * @param {string} bar Example description for bar.
 * @returns {string} Concatenated foo + bar.
 */
function foobar(foo, bar) {
	const concatenated = foo + bar;
	return concatenated;
}
